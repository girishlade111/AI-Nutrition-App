import React from 'react';
import { Button } from '@/app/components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/app/components/ui/card';
import { IconArrowLeft, IconApple, IconUploadCloud, IconWatch, IconUser, IconChevronRight } from '@/app/components/icons';
import { PAGES } from '@/app/lib/constants';

interface PageProps {
  onNavigate: (page: string) => void;
}

export const OnboardingPage = ({ onNavigate }: PageProps) => (
  <div className="p-6 bg-gray-50 min-h-screen">
    <Button variant="ghost" size="sm" onClick={() => onNavigate(PAGES.LANDING)} className="mb-4">
      <IconArrowLeft className="w-4 h-4 mr-2" /> Back
    </Button>
    <h2 className="text-2xl font-semibold text-gray-900">How should we start?</h2>
    <p className="text-gray-600 mb-6">Importing data gives the most accurate plan.</p>
    
    <div className="space-y-4">
      <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wide">Path A: Import Data (UI Only)</h3>
      <Card className="opacity-50 cursor-not-allowed">
        <CardHeader>
          <CardTitle className="flex items-center">
            <IconApple className="w-6 h-6 mr-3 text-black" />
            Connect Apple Health
          </CardTitle>
          <CardDescription>Automatically sync your health data.</CardDescription>
        </CardHeader>
        <CardContent>
          <div className="flex flex-col items-center justify-center p-6 border-2 border-dashed border-gray-300 rounded-md text-center">
            <IconUploadCloud className="w-10 h-10 text-gray-400 mb-2" />
            <span className="text-sm font-medium text-gray-600">Drag 'n' drop 'Health Export.zip'</span>
            <span className="text-xs text-gray-500 mt-1">or click to browse</span>
          </div>
          <Button variant="secondary" className="w-full mt-4" disabled>
            <IconWatch className="w-4 h-4 mr-2" /> Connect Wearable
          </Button>
        </CardContent>
      </Card>

      <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wide">Path B: Manual Entry</h3>
      <Card 
        className="border-2 border-orange-600 ring-2 ring-orange-600/20 cursor-pointer hover:bg-orange-50/50 transition-colors"
        onClick={() => onNavigate(PAGES.INTAKE_STEP_1)}
      >
        <CardHeader>
          <CardTitle className="flex items-center text-orange-700">
            <IconUser className="w-6 h-6 mr-3" />
            Start with Manual Entry
          </CardTitle>
          <CardDescription>Answer a few simple questions. (Takes ~2 mins)</CardDescription>
        </CardHeader>
        <CardFooter>
          <Button variant="link" className="text-orange-600 p-0">
            Continue with Manual Entry
            <IconChevronRight className="w-4 h-4 ml-1" />
          </Button>
        </CardFooter>
      </Card>
    </div>
  </div>
);
