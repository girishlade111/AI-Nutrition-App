// --- UTILITY (Mocking clsx/twMerge) ---
// In a real project, you would install `clsx` and `tailwind-merge`
// import { clsx, type ClassValue } from "clsx"
// import { twMerge } from "tailwind-merge"
// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }

// Using the simple mock from the prototype:
export const cn = (...classes: (string | boolean | undefined | null)[]) => classes.filter(Boolean).join(' ');
