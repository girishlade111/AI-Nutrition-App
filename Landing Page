import React from 'react';
import { Button } from '@/app/components/ui/button';
import { Badge } from '@/app/components/ui/badge';
import { IconChevronRight } from '@/app/components/icons';
import { PAGES } from '@/app/lib/constants';

interface PageProps {
  onNavigate: (page: string) => void;
}

export const LandingPage = ({ onNavigate }: PageProps) => (
  <div className="flex flex-col items-center justify-between min-h-screen p-6 bg-orange-50/50">
    <div className="w-full max-w-md text-center mt-16">
      <Badge variant="outline" className="border-orange-200 bg-orange-100 text-orange-700">
        पुण्यापासून मुंबईपर्यंत
      </Badge>
      <h1 className="text-4xl font-bold text-gray-900 mt-4">
        AI Nutrition,
        <br />
        <span className="text-orange-600">Made for Maharashtra.</span>
      </h1>
      <p className="text-lg text-gray-600 mt-4">
        Get healthy diet plans that understand your love for Poha, Bhakri, and Misal. Truly regional, just for you.
      </p>
    </div>
    
    <div className="w-full max-w-md p-4 mb-4">
      <img
        src="https://placehold.co/600x400/FFF0E6/F97316?text=Healthy+Maharashtrian+Thali&font=inter"
        alt="Healthy Maharashtrian Thali"
        className="rounded-xl shadow-lg w-full"
        onError={(e) => (e.currentTarget.src = 'https://placehold.co/600x400/FFF0E6/F97316?text=Healthy+Meal&font=inter')}
      />
    </div>
    
    <div className="w-full max-w-md p-4 sticky bottom-4">
      <Button
        className="w-full text-lg font-semibold"
        size="lg"
        onClick={() => onNavigate(PAGES.ONBOARDING)}
      >
        Get Started (सुरुवात करा)
        <IconChevronRight className="w-5 h-5 ml-2" />
      </Button>
    </div>
  </div>
);
